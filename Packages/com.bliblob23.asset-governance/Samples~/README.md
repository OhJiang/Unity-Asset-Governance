# Samples

- **Custom Rule and Strongly Typed Settings** demonstrates an independently compiled third-party
  rule, its own `ScriptableObject` configuration, and EditMode tests without modifying framework
  source code or a central rule list.
